/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['www.google.com', 'www.vapzvape.com'], // Adicione os domínios que você deseja permitir
    },
};

export default nextConfig;
